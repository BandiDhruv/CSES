#include <bits/stdc++.h>
using namespace std;
#define int long long
void tests() {
    int n,q;
    cin >> n >> q;
    vector<int> a(n+1);
    a[0]=0;
    for (int i = 1; i <= n; i++) cin >> a[i];
    while(q--){
        int u,v;
        cin>>u>>v;
        
    }
}
int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    tests();
    return 0;
}